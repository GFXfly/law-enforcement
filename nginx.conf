server {
    listen 80;
    server_name wenshu.site www.wenshu.site;

    # 访问日志
    access_log /var/log/nginx/law-enforcement-access.log;
    error_log /var/log/nginx/law-enforcement-error.log;

    # 客户端请求大小限制
    client_max_body_size 50M;

    # 安全设置
    server_tokens off;
    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-XSS-Protection "1; mode=block";
    add_header X-Content-Type-Options "nosniff";
    add_header Referrer-Policy "strict-origin-when-cross-origin";
    # 允许 HSTS (如果以后开启 HTTPS 请取消下面注释)
    # add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    location / {
        # 防止目录穿越
        if ($request_uri ~* "\.\./") {
            return 400;
        }

        # 后端应用地址 - 确保端口与实际运行端口一致
        proxy_pass http://127.0.0.1:3002;
        proxy_http_version 1.1;

        # WebSocket 支持
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';

        # 请求头设置
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 超时设置(增加到120秒)
        proxy_connect_timeout 120s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;

        # 缓冲设置
        proxy_buffering off;
        proxy_request_buffering off;
    }

    # 静态资源缓存
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        proxy_pass http://localhost:3002;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Next.js 静态文件
    location /_next/static/ {
        proxy_pass http://localhost:3002;
        expires 365d;
        add_header Cache-Control "public, immutable";
    }
}

# HTTPS 配置（SSL证书配置后启用）
# server {
#     listen 443 ssl http2;
#     server_name wwww.deepseek2046.com www.wwww.deepseek2046.com;
#
#     ssl_certificate /etc/nginx/ssl/wwww.deepseek2046.com.crt;
#     ssl_certificate_key /etc/nginx/ssl/wwww.deepseek2046.com.key;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     access_log /var/log/nginx/law-enforcement-ssl-access.log;
#     error_log /var/log/nginx/law-enforcement-ssl-error.log;
#
#     client_max_body_size 50M;
#
#     location / {
#         proxy_pass http://localhost:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_cache_bypass $http_upgrade;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#     }
#
#     location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
#         proxy_pass http://localhost:3000;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location /_next/static/ {
#         proxy_pass http://localhost:3000;
#         expires 365d;
#         add_header Cache-Control "public, immutable";
#     }
# }
#
# # HTTP 自动跳转 HTTPS
# server {
#     listen 80;
#     server_name wwww.deepseek2046.com www.wwww.deepseek2046.com;
#     return 301 https://$server_name$request_uri;
# }